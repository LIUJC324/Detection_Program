# 服务日志说明（2026-04-07）

日志目录：

- `/home/liujuncheng/rgbt_uav_detection/outputs/service_logs`

当前说明：

- `quick tunnel` 已下线
- 后续固定公网入口统一走 `http://47.108.223.231:18000`
- 服务日志继续只看本目录，不再依赖 `/tmp/rgbt_uav_cloudflared.log`

日志文件：

- `service.log`
- `session.log`
- `error.log`

当前已记录内容：

- 服务启动与关闭
- HTTP 请求访问
- 单帧检测接口调用
- 视频/流会话创建
- 会话探测成功
- 会话运行中进度
- 会话停止/完成/失败
- 回调失败
- 未处理异常

常用查看命令：

```bash
tail -f /home/liujuncheng/rgbt_uav_detection/outputs/service_logs/service.log
```

```bash
tail -f /home/liujuncheng/rgbt_uav_detection/outputs/service_logs/session.log
```

```bash
tail -f /home/liujuncheng/rgbt_uav_detection/outputs/service_logs/error.log
```

```bash
rg "session_id|sessionId|callback_failed|session_failed" /home/liujuncheng/rgbt_uav_detection/outputs/service_logs
```
